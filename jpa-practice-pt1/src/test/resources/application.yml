spring:
# spring boot 는 기본으로, 메모리 모드로 돌림(DB 안띄워도 X)
  #datasource:
   # url: jdbc:h2:mem:test
    #username: sa
    #password:
    #driver-class-name: org.h2.Driver

  #jpa:
    #hibernate:
     # ddl-auto: create-drop
    #properties:
    #  hibernate:
#        show_sql: true  system.out

   #     format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace
